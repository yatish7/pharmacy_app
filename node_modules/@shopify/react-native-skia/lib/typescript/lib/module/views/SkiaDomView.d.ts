export class SkiaDomView extends React.Component<any, any, any> {
    constructor(props: any);
    _nativeId: number;
    get nativeId(): number;
    componentDidUpdate(prevProps: any): void;
    componentWillUnmount(): void;
    tick(): void;
    requestId: number | undefined;
    /**
     * Creates a snapshot from the canvas in the surface
     * @param rect Rect to use as bounds. Optional.
     * @returns An Image object.
     */
    makeImageSnapshot(rect: any): import("../../..").SkImage;
    /**
     * Creates a snapshot from the canvas in the surface
     * @param rect Rect to use as bounds. Optional.
     * @returns An Image object.
     */
    makeImageSnapshotAsync(rect: any): Promise<import("../../..").SkImage>;
    /**
     * Sends a redraw request to the native SkiaView.
     */
    redraw(): void;
    render(): React.DetailedReactHTMLElement<React.InputHTMLAttributes<HTMLInputElement>, HTMLInputElement>;
}
import React from "react";
